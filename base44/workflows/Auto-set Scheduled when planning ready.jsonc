{
  "name": "Auto-set Scheduled when planning ready",
  "description": "Automatically transitions WorkOrder status to \"Scheduled\" when scheduled_date and assigned_technicians are set, and back to \"Draft\" when planning is incomplete. Only operates on Draft/Scheduled states, never overrides operational states (In Progress, Completed, etc.)",
  "trigger": {
    "condition": null,
    "config": {
      "trigger_type": "entity",
      "events": [
        "update"
      ],
      "entity_name": "WorkOrder"
    }
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "auto_set_scheduled_when_planning_ready",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "autoSetScheduledWhenReady",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  },
  "x-base44-migrated-from-automation": {
    "legacy_payload_function": "autoSetScheduledWhenReady"
  }
}